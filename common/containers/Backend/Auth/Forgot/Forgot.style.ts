import styled from "styled-components"

const ActionWrapper = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 20px;
`

export default {
  ActionWrapper,
}
